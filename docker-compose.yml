services:
  api:
    build: .
    ports: ["8000:8000"]
    volumes: ["./data:/app/data"]
    environment:
      - DATA_DIR=/app/data
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    environment:
      - VITE_API_URL=http://localhost:8000/api
    depends_on: [api]
