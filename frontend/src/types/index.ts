export type VehicleState={vehicle_id:string;route_id:string;trip_id:string;mode:string;lat:number;lon:number;heading:number;speed_mps:number;next_stop_id:string|null;next_stop_name:string|null;progress:number;distance_m:number};
export type SimState={playing:boolean;date:string;time:string;speed:number;showMetro:boolean;showTram:boolean};
